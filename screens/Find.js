import React from 'react';
import { View, Text, Button, ScrollView, StyleSheet, Image } from 'react-native';

import mm from '../assets/carsMain.jpg';

const Find = ({ navigation }) => {
   return (
       <View></View>
   );
}

export default Find;